import * as Lucide from 'lucide-react';
console.log(Object.keys(Lucide).filter(k => k.toLowerCase().includes('facebook')));
console.log(Object.keys(Lucide).filter(k => k.toLowerCase().includes('twitter')));
console.log(Object.keys(Lucide).filter(k => k.toLowerCase().includes('instagram')));
